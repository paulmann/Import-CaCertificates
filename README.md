# Import-CaCertificates
PowerShell 7.5+ script to import public CA root and intermediate certificates into the Windows LOCAL MACHINE trusted certificate store. Supports Let's Encrypt, PEM/DER formats, WhatIf, duplicate prevention.
